[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: cualquier ruta sirve index.html para que el router del
# cliente resuelva /dashboard, /tropels, /signals, /sectors/:id/story, etc.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
